import 'package:flutter/material.dart';

class HomeScreenAref extends StatelessWidget {
  const HomeScreenAref({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blueGrey,
      body: SafeArea(
        child: Text('Aref'),
       ),
    );
  }
}
